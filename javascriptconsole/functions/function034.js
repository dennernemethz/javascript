// EX.01 
//usaremos map methodo dentro da função capaz de modificar elementos de arrays 

let color = ['cinza','verde','azul']

let r = color.map((el,i)=>{
    console.log(el)  
})

// el e o primeiro item da lista de arrays para modificar o conteudo do elemento
// se caso mandarf mostrar o i vai mostar a posição dos elementos dentro de arrays cores
